spring.application.name=college-directory
# Server port (default is 8080)
server.port = 8080

# PostgreSQL database connection settings
spring.datasource.url = jdbc:postgresql://localhost:5432/college_directory
spring.datasource.username =  postgres
spring.datasource.password =  Satyam@2504
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update #Use update to automatically create/update the schema
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

